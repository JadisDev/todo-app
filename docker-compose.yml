version: "3"

services:
   db:
    image: mongo
    container_name: mongodb
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin

  api:
    image: back-fortbrasil
    container_name: back-fortbrasil
    build: .
    env_file:
      - ./.env
    ports:
      - 3333:3333
    restart: always
    stdin_open: true
    tty: true
